# Evidence-driven hunter workflow

`aletheia hunt` accepts only a locally registered, explicitly in-scope target.
It snapshots scope, maps source spans, creates a bounded plan, scans, and
creates a human-review queue. It never submits reports or executes production
transactions. Candidates cannot be promoted by AI.
