# Capability matrix

Use `aletheia inventory` for live tool and ecosystem status. Non-EVM semantic
parsers are `partial`: they create source-mapped candidates, while native
reproduction remains optional and required for verification.
