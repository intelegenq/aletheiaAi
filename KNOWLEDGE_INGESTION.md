# Knowledge ingestion

Only import locally supplied, legally usable data with source, license/usage
flag, hash, import date, and confidence. Historical cases inform hypotheses;
they never prove a finding or alter its verification status.
